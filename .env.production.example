# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES FOR COOLIFY DEPLOYMENT
# =============================================================================
# Copy this file to your Coolify environment variables section
# Replace all placeholder values with your actual production values

# Application Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://postgres:your_password@postgres:5432/primesms
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=primesms
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_database_password

# Security Configuration
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters
SESSION_SECRET=your_session_secret_key_here_minimum_32_characters

# Meta WhatsApp Business API Configuration
META_ACCESS_TOKEN=your_meta_access_token_from_facebook_app
META_PHONE_NUMBER_ID=your_whatsapp_phone_number_id
META_VERIFY_TOKEN=your_webhook_verify_token
META_APP_SECRET=your_facebook_app_secret
META_APP_SECRET_2=optional_second_app_secret_for_multiple_apps

# Webhook Configuration
WEBHOOK_DEBUG_TOKEN=your_webhook_debug_token_for_testing
N8N_WEBHOOK_URL=your_n8n_webhook_url_if_using_n8n_integration

# API Configuration
GRAPH_API_VERSION=v22.0

# Deployment Flags
COOLIFY_DEPLOYMENT=true

# =============================================================================
# COOLIFY DEPLOYMENT NOTES:
# =============================================================================
# 1. Set up these environment variables in your Coolify project settings
# 2. PostgreSQL will be automatically provisioned via docker-compose
# 3. Health checks are configured to monitor /health endpoint
# 4. SSL certificates will be automatically managed by Coolify
# 5. Domain: primesms.app and www.primesms.app (update in coolify.json)
# 6. Auto-deployment is enabled for main branch commits
# =============================================================================